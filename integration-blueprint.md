# PMR 前台、客服後台、Google 試算表與 LINE 串接藍圖

## 建議的工作方式

社群貼文只負責吸引客人。客人按下專案連結後，統一進入 LINE 官方帳號內的預訂前台；系統帶入 LINE 身分、專案代碼與貼文來源。訂單建立後，客服在營運後台更新付款、補件、額度、出票、加購與內部備註，客人則在「我的訂單」查看自己可見的進度。

```text
LINE 社群／一般群組貼文
        ↓ 專案連結 + campaign 來源碼
LINE 官方帳號／LIFF 客戶前台
        ↓ 建立訂單
PMR 訂單資料庫
   ↙              ↘
客服營運後台      客戶「我的訂單」
   ↓              ↑
付款、補件、額度、出票、加購、分潤
        ↓
Google 試算表（查帳、匯出、備份）
```

## 現有試算表盤點

目前有兩張工作表：

- 「表單回覆 1」：時間戳記、預定專案、預定張數、訂購人、手機、艙等、星宇加價、出發／抵達日期、許願航班、護照、匯款截圖、匯款金額、介紹人、備註。
- 「利潤」：訂購人、預定張數、匯款金額、訂購成本、客服費用、介紹人、介紹人利潤、資源方利潤。

最大風險是兩張表用「姓名」對資料。正式串接前應先增加不可重複的 `order_id`，所有表都靠訂單編號關聯。

## 正式資料表設計

### 1. Projects 專案

`project_id`、標題、可選目的地、公開／代碼限定／下架、售價、單張成本、每組張數、庫存、效期、是否允許不同點進出、行李與其他特殊條件、發布日期。

### 2. Customers 客戶

`customer_id`、LINE user ID、LINE 顯示名稱、真實姓名、電話、最後互動時間、來源標籤、同意紀錄。護照不直接存在試算表，只放受權限保護的檔案連結。

### 3. Orders 訂單

`order_id`、`customer_id`、`project_id`、購買組數、購買張數、艙等、訂單售價、效期、付款狀態、補件狀態、出票狀態、負責客服、介紹人、下一步日期、客戶可見備註、內部備註。

### 4. Credits 額度

`order_id`、購買張數、處理中張數、已出票張數、退款張數、剩餘張數。剩餘張數由公式計算：

`已付款購買張數 - 處理中 - 已出票 - 已退款`

### 5. Travelers／Documents 旅客與文件

每一位旅客一列，記錄姓名、證件補齊狀態、許願航班狀態、使用哪一張額度、實際出票資料與受保護檔案連結。

### 6. Payments 付款

`payment_id`、`order_id`、金額、匯款後五碼、明細連結、核對狀態、核對人、核對時間、退款金額與退款帳號後五碼。

### 7. AddOns 加購

每個加購項目一列：名稱、售價、實際成本、數量、毛利、供應商與狀態。四人房、機場接送等都不應混在原機票成本中。

### 8. Profit 分潤

訂單收入、方案成本、客服費、加購毛利、介紹人分潤、資源方分潤、未分配差額。每次修改保留操作人與時間。

### 9. ActivityLog 操作紀錄

記錄何人於何時把什麼欄位從何值改成何值；付款、退款、剩餘額度與分潤不可只留下最新結果。

## 訂單狀態建議

```text
等待客服確認 → 等待付款 → 款項待核對 → 資料補件中
→ 許願航班確認 → 準備出票 → 已出票／部分使用 → 已完成
```

取消、退款與異常另外走獨立分支，不能覆蓋原付款紀錄。

## LINE 串接原則

- LINE 官方帳號：使用 Messaging API 接收訊息與推播提醒；LIFF 前台取得 LINE user ID，讓客戶只能看到自己的訂單。
- 一般 LINE 群組：官方帳號開啟允許加入群組後，可接收群組 Webhook。每個群組只放一個官方帳號機器人。
- LINE OpenChat 社群：不依賴社群成員身分做訂單關聯。貼文附帶追蹤連結，導向官方帳號／LIFF 完成預訂。
- 每篇貼文連結加入 `project_id` 與 `campaign_id`，才能知道哪一個專案、哪一個群組與哪一天真正帶來成交。

## 自動提醒規則

- 建立訂單 2 小時仍未付款：提醒客服跟進，不直接催客人。
- 款項確認後 24 小時仍未補護照：官方帳號傳一次溫和提醒。
- 出發前 30、14、7 天：檢查護照、航班、出票狀態與特殊需求。
- 專案效期前 90、30 天：提醒客人仍有多少張可用。
- 每天 09:00：產生新專案草稿，先交由人工核准，再發布社群與官方帳號。

## 正式上線需要的授權

1. Google：建立可由後端讀寫的 Google Cloud 專案與 Sheets／Drive 權限，或改用正式資料庫並只同步摘要到試算表。
2. LINE：在官方帳號啟用 Messaging API，建立 Provider、Channel、LIFF app、Webhook URL 與 Channel access token。
3. 網站：部署 HTTPS 網域；設定客服登入與角色權限，不使用共用密碼。
4. 資料保護：護照與匯款明細使用私有檔案儲存、限時連結、存取紀錄與保留期限；不把圖片公開連結放在客戶頁。

## 建議分三階段上線

1. **試跑版**：以新資料表承接新訂單；舊資料只讀；2 位客服試用 3～5 天。
2. **LINE 版**：接上 LIFF 與官方帳號提醒；社群貼文改用可追蹤專案連結。
3. **財務版**：加入付款核對、退款、加購、分潤結算與完整稽核紀錄，再逐步匯入舊訂單。
